# Task receipt: R13 Sonic1 real permission smoke — 2026-09-16

Task ID / parent milestone: `R13` / `R00`.

Scope: exercise the current Linux x86-64 `clamd`/`clamonacc` pair against a
real unprivileged open in a disposable privileged Docker namespace. The smoke
covers prevention clean, detection, and limit outcomes plus a separate
monitoring-only detection outcome. It is development evidence only and does
not claim the full six-case R13 matrix or release qualification.

## Runtime identity and admission boundary

- host: Sonic1, Linux `x86_64`;
- image: `clamav-32gb:dev-current`, image ID
  `sha256:c0c10e2d6e6675c201dc657276543462de64896e53ae44cb9ba725a3a12d86df`;
- source container: `clamav-current-source-20260915`, source HEAD
  `08b3ab820e40ac8bd307290b0c78c03c94884f1b`, dirty checkout;
- each runtime used `docker run --rm --privileged`, a private Docker tmpfs at
  `/watch`, and `OnAccessIncludePath /watch` with a root-excluded setup actor;
- clamd and clamonacc were both run in the foreground under tracked MCP-SSH
  jobs with the documented 600-second effective async limit.

The first full-profile start was rejected by the application admission gate:

```text
temporary_free=57154293760 bytes is below required=73014444032 bytes
```

This is the expected Sonic1 resource shortfall for the full 64-GiB temporary
contract. The following smoke runs used `MaxTemporarySize 40G` explicitly as a
development-only reduction; no qualification setting was changed.

## Prevention-mode observations

The privileged runtime container was
`e13b1b892ad490d6361c2f5fc53da173c5009cb9e8adc1783d3226d379f67230`, with
clamd job `job_971bec28944041dc99802b94b0892673` and clamonacc job
`job_fb6b90dc33b74cfd9f829a0ae175d57c`.

- Clean fixture `/watch/clean2.txt`: an unprivileged actor opened it
  successfully and read `b''`.
- Detection fixture `/watch/detection.bin`: the current scoped signature
  `LargeFile.Zip.32G.LateMember.UNOFFICIAL` was logged as `FOUND`; clamonacc
  logged `permission scan detected malware`, and the unprivileged actor
  received `PermissionError` with errno `1` (`EPERM`). The open was denied.

The clean and detection events were generated by separate unprivileged
processes; fixture creation was performed as excluded root before each open.

## Limit-mode observation

The one-byte on-access limit runtime was
`c8b713f88e784b8b6b9d6f5a4a4bf77cdcaa3fae8c41ca68d4095ccd96d499e4`, with
clammacc job `job_81695d599669416180658e7ee1c56ad0` and clamd job
`job_e4ed64a317c246699a1cf6aca148e20a`.

The two-byte `/watch/limit.bin` fixture was opened by an unprivileged actor
and received errno `1` (`EPERM`). The retained clamonacc output was:

```text
ClamWorker: permission scan of '/watch/limit.bin' was incomplete (status 24)
```

This demonstrates the current prevention path denies a real limit-incomplete
permission event rather than allowing it as clean.

## Monitoring-only observation

The monitoring-only runtime was
`bfde27ee92bade29ffd40f401607de65a6d82fae81214ecfd8de71a2acc4899f`, with
clamonacc job `job_ababe13f938d4b35a4737658ecf60dfa` and clamd job
`job_e34c569a37db425498abd62a10860c8f`.

The unprivileged actor opened `/watch/monitor-detection.bin` successfully and
read the marker bytes. Clamonacc logged:

```text
/watch/monitor-detection.bin FOUND
ClamWorker: on-access scan detected malware in '/watch/monitor-detection.bin'
```

The event was therefore allowed in monitoring-only mode without being labeled
clean.

## Cleanup and qualification boundary

All clamonacc and clamd jobs were canceled by their exact job handles. The
three named disposable containers were explicitly stopped, verified absent
from `docker ps -a`, and the temporary staged binaries, database, logs,
configuration, and runtime directory were removed. No source-tree or
production deployment state was changed.

These are genuine runtime observations, but they do not provide retained
kernel-event artifacts, event IDs, sanitizer/resource proof, parser/resource/
timeout cases, a certified immutable build, or the complete monitoring matrix.
R13 remains open and no capability was promoted.

State: `development-smoke-verified; full-permission-evidence-open`
